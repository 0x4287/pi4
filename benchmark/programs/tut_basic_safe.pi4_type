(x:{y:⊤|
    y.pkt_out.length == 0 ∧
    !y.ether.valid ∧
    !y.ipv4.valid ∧
    y.pkt_in.length > 272 ∧
    y.stdmeta.valid ∧
    y.forward_table.valid
    }) -> {y:⊤ | (y.stdmeta.drop == 0b1) => (y.pkt_out.length == 0)}
